# ═══════════════════════════════════════════════════════════════════════════
# SDXL PEFT Integration Test: Core
# ═══════════════════════════════════════════════════════════════════════════
# Usage: python scripts/sdxl_peft.py --config-name=test_core

defaults:
  - benchmark_sdxl
  - _self_

output:
  saving:
    output_dir: "D:/Projects/sd-scripts/tests/test_output"
    output_name: "test_core"
  logging:
    logging_dir: "D:/Projects/sd-scripts/tests/test_logs"

data:
  caching:
    cache_dir: "D:/Projects/sd-scripts/benchmark_cache"
