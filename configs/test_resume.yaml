# ═══════════════════════════════════════════════════════════════════════════
# SDXL PEFT Integration Test: Resume
# ═══════════════════════════════════════════════════════════════════════════
# Usage: python scripts/sdxl_peft.py --config-name=test_resume
# Prerequisite: Run test_checkpoint first to create the state directory

defaults:
  - test_core
  - _self_

output:
  saving:
    output_name: "test_resume"
    resume: "D:/Projects/sd-scripts/tests/test_output/test_checkpoint-step00000020-state"
