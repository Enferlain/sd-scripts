# @package _global_
# ═══════════════════════════════════════════════════════════════════════════
# SDXL PEFT Integration Test: Validation
# ═══════════════════════════════════════════════════════════════════════════
# Usage: python scripts/sdxl_peft.py --config-name=tests/test_validation

defaults:
  - /tests/test_core
  - _self_

validation:
  validation_split: 0.1
  validate_every_n_steps: 10
  validation_seed: 42
  run_at_start: true
  run_at_end: true
