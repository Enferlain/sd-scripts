Production implementation is **not ready**. Sections 1–5 are the numbered
OpenSpec design milestones required by `design.md` G1–G5. They change planning
artifacts and evidence only. Exact production milestones are added in section 5
after the exchanges, representative cases, overlap reconciliation, tests, and
review gates are complete. OpenSpec artifact status does not override this
boundary. Bead `sd-scripts-syv` remains the durable tracker.

## 1. Complete Carry-Forward And Contract Reconciliation

- [x] 1.1 Build a source-section-to-requirement matrix covering the normative direction, EX-001–EX-031 dispositions, Q1–Q5, identity/resume, preparation publication, optimization identity/non-overlap, artifact products, and the topology correction; verify every settled item points to a design decision and delta requirement and no superseded wording is promoted.
- [x] 1.2 Audit all affected main specs and `expand-model-metadata-identity-and-structure`; record every contradiction, unchanged dependency, and explicit delta in `design.md`. Preserve the broader metadata requirements while making this rework authoritative for overlapping runtime semantics; record concrete milestone dependencies without assuming that either entire change must land first. Reconcile the metadata delta that makes Trainer retain the primary loading/provenance state with this change's accepted-arrangement ownership, and reconcile its optimization-only target-building actor and unqualified live-parameter behavior with shared policy-neutral refs and revision-pinned consumer projections. Include every inherited optimization-owned adapter-targeting assumption, every `AdapterMode`-gated method/observability scenario, and the disposition of migration-scoped adapter-target and selector requirements. Treat normalization of the malformed delta-shaped `repo-owned-vera-method` main spec as a hard prerequisite for the validation checkbox; verify `openspec validate rework-model-strategy-trainer --strict` passes.
- [x] 1.3 Refresh the intended-Trainer evidence across launcher, setup, model preparation, optimizer preparation, loop, checkpoint/restoration, and capability orchestration; verify exact source snippets and code-graph traces support every current-state claim and coverage is checked for each relied-on path.
- [x] 1.4 Expand D5 into the common Trainer-consumption frame for strategy fulfillment, materialization/binding, preparation, optimization, execution, caching, validation, sampling, persistence, and restoration; identify the request-input, result, readiness/evidence, state-effect, external-effect, and failure categories every eventual exchange must make explicit, and verify every row uses one authoritative revisioned obligation model, checks facts at its earliest authoritative availability, maps to at least one normative requirement, and adds no family-shaped universal inputs or independent validator authority. Name the owner that derives each governed-realization job and the coordinator of final publication across authority-owned and Trainer-owned state. For capability rows, stop at this common responsibility/evidence frame; tasks 4.1–4.4 complete their domain-specific exchanges after execution and optimization semantics are settled.
- [x] 1.5 Validate the milestone, discuss the reconciled architecture with the user, obtain the required review, address actionable findings, and record the accepted next evidence scope in bead `sd-scripts-syv`.

## 2. Derive Accepted Execution From Representative Cases

- [x] 2.1 Trace ordinary SDXL fine-tune materialization, preparation, training, validation, sampling, and persistence against D5; include scheduled behavior and an observation-driven adaptive path, and verify the design identifies each retained Trainer mechanic, accepted domain operation, changing input, owned-state dependency, result, failure, and displaced current owner with exact source evidence.
- [x] 2.2 Trace SDXL PEFT adoption, declared current method support, selected-method construction, strategy-authored semantic target intent, governed target resolution, attachment, PEFT-only and joint base-plus-PEFT optimization subjects, specialized lifecycle behavior, artifact export, artifact-based new-run initialization, and exact restoration; verify shared integration behavior is separated from genuine method differences, target refs remain policy-neutral, parameter grouping begins only after adapter realization returns trainables, participant, lineage, relationship, route/view, operation/capability, and optimization meanings are explicit, and no `AdapterMode`-shaped authority remains.
- [x] 2.3 Compare SD and SD3, including component-count differences, typed conditioning, objective restrictions, component-specific preparation, unsupported products, and deferred materialization; check for SDXL-only assumptions in the accepted execution meanings without treating related SD families as proof of generality.
- [ ] 2.4 Work through teacher/student distillation, pixel or other non-latent representation, video/audio or another materially different tensor shape, an added side-network or LLM-containing case, and one lifecycle-driven authority-governed transition; verify distinct identity despite shared sources, asymmetric roles/routes, representation and axis semantics, relationships, readiness, optimization, effects, transition/republication, and products are representable without new universal Trainer slots or image-shaped fields.
- [ ] 2.5 Work through one stateful or adaptive custom structured operation and one strong imperative research region that requests actual backward, gradient, or advancement authority; verify standard-profile acceptance, runtime-state ownership/restoration, extension-profile acceptance, unsupported-authority rejection, and prevention of dual ownership are specified.
- [ ] 2.6 Select the minimum accepted operation/region, dependency, changing-input, owned-state, bounded-decision, effect, observation, transition, failure, and authority meanings supported by all cases; derive initialization, freshness, invalidation, persistence, and restoration rules for operation-owned state, including what happens when its participant, binding, route, relationship, or observation dependencies change; and verify readable authoring and execution examples, hot-path boundaries, and a rationale comparing graph, region, schedule, lowered-Python, and hybrid options are recorded without choosing by analogy alone or requiring a frozen linear plan.
- [ ] 2.7 Update `accepted-training-execution`, `training-contract`, and affected capability specs from the case findings; validate strictly, obtain the required review, address findings, and receive user confirmation before starting optimization/step completion.

## 3. Complete Optimization And Step Exchanges

- [ ] 3.1 Define the declarative advancement policies supported by the standard Trainer-owned profile, including multiple coordinated units and ordinary unusual cadence; verify each policy states backward, clipping, synchronization, advancement, zeroing, lifecycle, checkpoint, and failure ownership.
- [ ] 3.2 Define the explicit extension boundary for changed optimization authority; verify the strong imperative case names transferred actions, retained Trainer actions, allowed state access, effects, failure/restoration rules, and rejection of duplicate ownership without assuming the authored strategy is the executor.
- [ ] 3.3 Define backend-flexible optimization plan/candidate/request/result meanings across joint preparation; verify both optimizer-before-wrapper and wrapper-before-optimizer backend orders preserve unit identity, source dependencies, non-overlap, and one complete publication boundary.
- [ ] 3.4 Define invalidation and replanning for binding, relationship, trainability, parameter-surgery, and policy changes; verify same-revision re-realization, revised definitions, new unit identities, stale-result rejection, and exact restoration scenarios are covered.
- [ ] 3.5 Complete the step exchange between accepted execution and optimization; verify changing runtime inputs, bounded decisions, operation-owned state updates, operation/region outputs, optimization inputs, observations, authority-governed transition requests, advancement results, and failures are explicit without requiring one loss producer, one differentiable result, one advancement sequence, or diffusion timesteps.
- [ ] 3.6 Add conformance and representative performance acceptance criteria for the standard and extension paths; verify static selection/validation is outside the repeated hot path without resolving scheduled or adaptive behavior to fixed authoring-time outcomes, allow observably equivalent lowering where valid, and measure runtime dispatch or state-transition cost against the current loop.
- [ ] 3.7 Update `training-optimization`, `accepted-training-execution`, preparation, and contract specs; validate strictly, obtain the required review, address findings, and receive user confirmation that the four core exchanges are semantically complete.

## 4. Complete Recognized Capability Exchanges And Metadata Boundaries

- [ ] 4.1 Complete caching request/result/readiness/dependency/failure semantics; verify representation and conditioning codecs remain selected behavior while data traversal/storage remains pipeline-owned and stale cache state cannot survive dependency changes.
- [ ] 4.2 Complete validation and sampling exchanges; verify Trainer/pipeline owns trigger, traversal or destination, ordinary runtime modes, aggregation and observation while accepted domain behavior owns evaluation or generation semantics.
- [ ] 4.3 Complete trained-artifact persistence exchanges for current SDXL, SD3, adapter, and learned-sidecar products; verify declaration/request/plan/result, product/member/resource, transformations, dependencies, consistency, partial failure, actual post-write facts, and success independent of a simultaneous runtime-snapshot outcome are covered. Distinguish adapter product publication from loading that product to initialize a new participant with lineage.
- [ ] 4.4 Complete exact runtime restoration exchanges; verify Trainer/pipeline coordination and contributor-owned operation, capability, optimization, backend, and infrastructure state restore authority, participant, relationship, route, progress, and revisions distinctly from artifact loading, report an outcome independent from trained-product persistence, and use new execution-session identity without inventing new same-run participant incarnations.
- [ ] 4.5 Reconcile metadata and observability consumers with the completed runtime identities and results; verify neither system infers live identity, retains canonical bindings, or reconstructs topology from mode/strategy/Trainer compatibility fields.
- [ ] 4.6 Update capability, persistence, metadata, observability, adapter, and loaded-component deltas; validate strictly, obtain the required review, address findings, and close every overlap item recorded in milestone 1.

## 5. Choose Production Shapes And Write The Migration Plan

- [ ] 5.1 Derive concrete participant, relationship, binding, route, view, obligation-revision, evidence, snapshot, transition, realization-attempt, preparation-attempt, and publication types from the completed consumers; verify examples preserve every settled invariant without copying provisional spike names, exposing unrestricted lookup, or allowing downstream reinterpretation of the authored strategy.
- [ ] 5.2 Derive concrete semantic optimization plan, unit, candidate, prepared runtime, advancement, and checkpoint types; verify optimizer/backend objects remain realizations and all current flags, primary-trainable, synchronization-handle, and repeated mode queries have one intended replacement.
- [ ] 5.3 Choose the accepted-arrangement and executable-run representation from the case comparison; verify it makes semantic acceptance distinct from executable readiness and preserves the authoritative obligation revision, maintained/custom structure, imperative regions, capability state, binding authority, prepared routes, optimization runtime, readability, and hot-path performance boundaries.
- [ ] 5.4 Choose the training-contract definition, contract-guided authored-strategy, strategy-fulfillment boundary, and API; verify the active contract is available before authoring, fulfillment produces one authoritative run-specific obligation set and detailed pass/fail result, governed realization consumes that set without reinterpretation, bypass is prevented in the normal launcher, explicit composition remains understandable, and no automatic resolver or active-strategy callback is introduced. Derive the best representation and code placement for contract knowledge, shared conformance logic, fulfillment, evidence evaluation, and diagnostics without assuming one giant contract object or several independent validator systems.
- [ ] 5.5 Map final types and owners to production modules; verify dependency direction, public/internal boundaries, names, and migration from `LoadedModelComponent`, `TrainingStrategy`, `TrainingMode`, objectives, phases, checkpointing, metadata, and observability are recorded with no circular or duplicate authority. The migration MUST define the reusable PEFT authoring integration and bounded per-strategy method support; place semantic adapter target intent, governed concrete target resolution, policy-neutral target refs, method realization, and Trainer-owned parameter grouping without deriving ownership from the current `optimization` namespace; dissolve `FineTuneMode` into authored training-subject intent plus standard optimization/preparation; keep artifact-product choice independent; permit contract-compatible direct-plus-PEFT composition; and introduce no replacement runtime mode axis. Verify any transitional user-facing mode/preset label is consumed before the accepted-arrangement boundary.
- [ ] 5.6 Replace the planning-only production outline with exact numbered vertical implementation milestones; verify each milestone names changed paths, displaced authority removed in the same slice, governing requirements/scenarios, regression/conformance/failure/performance tests, rollback boundary, and required review gate. If old and new launch paths coexist temporarily, verify they select separate run topologies and that a new-topology run cannot fall through to active-strategy callbacks, `TrainingMode` ownership, or old Trainer binding state.
- [ ] 5.7 Run strict validation and documentation checks, obtain the required review, address findings, and discuss the complete production design and task breakdown with the user; only then mark the change ready for a separately requested production apply workflow.
